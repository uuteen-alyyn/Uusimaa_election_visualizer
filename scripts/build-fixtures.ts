/**
 * Build-time prefetch — Phase 0 stub.
 *
 * Phase 1 will wire this to the elections submodule's loaders and
 * emit JSON fixtures under public/data/elections/. For now, exits 0
 * so `npm run build` succeeds.
 */
console.log("[prefetch] stub — Phase 1 will populate public/data/elections/");
