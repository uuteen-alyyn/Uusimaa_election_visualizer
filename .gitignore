# Dependencies
node_modules/

# Build output
dist/
build/
.vite/

# Build artifacts: prefetched election fixtures + geometry copy
# (regenerated by `npm run prefetch`; sources live in /data/ and the
# elections submodule). Single source of truth, no manual sync needed.
public/data/elections/
public/data/fi-vaalipiirit.json
public/data/fi-kunnat.json

# Cache store written by the elections submodule's PxWeb client (runtime cache,
# regenerated automatically; survives across runs to avoid re-hitting PxWeb)
cache-store.json
.cache/

# Environment / local config
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test coverage
coverage/
